import React from 'react';
const SSOAuth = () => {
    return (
        <div className="min-h-screen flex justify-center items-center">
            <p>Redirecting for SSO authentication...</p>
        </div>
    );
};

export default SSOAuth;
